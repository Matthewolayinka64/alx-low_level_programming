#include <unistd.h>

/**
* _putchar - writhe the character c stdout
* @c: The character to print
*
* Return: on success 1.
* on error -1, is returned and errno is correct appropraitely.
*/
int _putchar (char c)
{
  return (write(1, &c, 1));
}

